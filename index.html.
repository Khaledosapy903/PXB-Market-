<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PXB Market</title>
    
    <script src="https://sdk.minepi.com/pi-sdk.js"></script>
    
    <script>
        // تهيئة التطبيق
        Pi.init({ version: "2.0", sandbox: false });

        async function makePayment() {
            try {
                const payment = await Pi.createPayment({
                    amount: 0.1,
                    memo: "تفعيل الخطوة 10 - PXB Market",
                    metadata: { step: 10 }
                }, {
                    onReadyForServerApproval: (paymentId) => { console.log("Approved", paymentId); },
                    onReadyForServerCompletion: (paymentId, txid) => { console.log("Completed", txid); },
                    onCancel: (paymentId) => { alert("تم إلغاء العملية"); },
                    onError: (error) => { alert("خطأ: " + error.message); }
                });
            } catch (err) {
                alert("يرجى فتح الرابط من داخل متصفح Pi Browser");
            }
        }
    </script>
</head>
<body style="text-align: center; font-family: sans-serif; padding-top: 50px;">
    <h1>مرحباً بك في PXB Market</h1>
    <p>اضغط على الزر أدناه لإتمام عملية الدفع التجريبية</p>
    
    <button onclick="makePayment()" style="padding: 15px 30px; background-color: #673ab7; color: white; border: none; border-radius: 8px; font-size: 18px; cursor: pointer;">
        تفعيل الخطوة رقم 10 (0.1 Pi)
    </button>
</body>
</html>

